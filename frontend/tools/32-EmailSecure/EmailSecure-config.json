{
  "toolName": "emailsecure",
  "tagline": "Email Security",
  "subdomain": "EmailSecure.maula.ai",
  "port": 3032,
  "apiPort": 4032,
  "mlPort": 8032,
  "aiPort": 6032,
  "databaseName": "EmailSecure_db",
  
  "systemPrompt": "You are emailsecure AI, an expert assistant for Email Security. You help users analyze, detect, and respond to security threats with advanced AI capabilities.",
  
  "functions": [
    {
      "name": "analyze_data",
      "description": "Analyze input data using emailsecure capabilities",
      "parameters": {
        "type": "object",
        "properties": {
          "data": {
            "type": "string",
            "description": "Data to analyze"
          }
        },
        "required": ["data"]
      }
    },
    {
      "name": "get_results",
      "description": "Retrieve analysis results",
      "parameters": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "description": "Result ID"
          }
        },
        "required": ["id"]
      }
    },
    {
      "name": "export_report",
      "description": "Generate and export analysis report",
      "parameters": {
        "type": "object",
        "properties": {
          "format": {
            "type": "string",
            "enum": ["pdf", "csv", "json"],
            "description": "Export format"
          }
        },
        "required": ["format"]
      }
    }
  ],
  
  "colorTheme": {
    "primary": "#3b82f6",
    "secondary": "#8b5cf6",
    "accent": "#06b6d4",
    "background": "#0a0e27",
    "surface": "#1a1f3a",
    "text": "#ffffff"
  },
  
  "navigationItems": [
    {
      "label": "Dashboard",
      "path": "/dashboard",
      "icon": "LayoutDashboard"
    },
    {
      "label": "Analysis",
      "path": "/analysis",
      "icon": "Search"
    },
    {
      "label": "Results",
      "path": "/results",
      "icon": "FileText"
    },
    {
      "label": "Settings",
      "path": "/settings",
      "icon": "Settings"
    }
  ]
}
