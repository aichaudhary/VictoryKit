# 🏗️ Base Architecture - MAULA.AI

## 🌐 System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                         CLOUDFLARE                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐             │
│  │   DNS        │  │   CDN        │  │  DDoS Shield │             │
│  └──────────────┘  └──────────────┘  └──────────────┘             │
│                     maula.ai                                        │
└─────────────────────────┬───────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    AWS EC2 (Ubuntu Server)                          │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                     NGINX (Reverse Proxy)                    │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │   │
│  │  │ maula.ai │  │api.maula │  │tool1.api │  │tool2.api │   │   │
│  │  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘   │   │
│  └───────┼─────────────┼─────────────┼─────────────┼──────────┘   │
│          │             │             │             │              │
│  ┌───────▼─────────────▼─────────────▼─────────────▼──────────┐   │
│  │              DOCKER CONTAINER LAYER                         │   │
│  │  ┌──────────────────────────────────────────────────────┐  │   │
│  │  │  Frontend Container (Next.js)                        │  │   │
│  │  │  ├─ Main Dashboard (VictoryKit UI)                   │  │   │
│  │  │  ├─ Tool 1 Frontend                                  │  │   │
│  │  │  ├─ Tool 2 Frontend                                  │  │   │
│  │  │  └─ ... (50 tool frontends)                          │  │   │
│  │  └──────────────────────────────────────────────────────┘  │   │
│  │  ┌──────────────────────────────────────────────────────┐  │   │
│  │  │  Backend Containers (Node.js + Python)               │  │   │
│  │  │  ├─ Auth Service (Node.js)                           │  │   │
│  │  │  ├─ API Gateway (Node.js)                            │  │   │
│  │  │  ├─ Tool 1 Backend (Node.js + Python)                │  │   │
│  │  │  ├─ Tool 2 Backend (Node.js + Python)                │  │   │
│  │  │  └─ ... (50 tool backends)                           │  │   │
│  │  └──────────────────────────────────────────────────────┘  │   │
│  │  ┌──────────────────────────────────────────────────────┐  │   │
│  │  │  AI Assistant Containers                             │  │   │
│  │  │  ├─ LLM Router (multi-provider)                      │  │   │
│  │  │  ├─ Tool 1 AI Instance                               │  │   │
│  │  │  ├─ Tool 2 AI Instance                               │  │   │
│  │  │  └─ ... (50 AI assistants)                           │  │   │
│  │  └──────────────────────────────────────────────────────┘  │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │              OpenVPN Cloud Connexa (Optional)               │   │
│  │              Secure Admin Access                            │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────┬───────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────────┐
│                     MONGODB ATLAS (Cloud)                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐             │
│  │  Auth DB     │  │  Tool 1 DB   │  │  Tool 2 DB   │  ... (50)   │
│  │  (Users,     │  │  (Logs,      │  │  (Scores,    │             │
│  │   Keys,      │  │   Data,      │  │   Records,   │             │
│  │   Sessions)  │  │   History)   │  │   Sessions)  │             │
│  └──────────────┘  └──────────────┘  └──────────────┘             │
└─────────────────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    LLM PROVIDERS (External APIs)                    │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ │
│  │Anthropic │ │ OpenAI   │ │   xAI    │ │ Mistral  │ │  Google  │ │
│  │  Claude  │ │   GPT    │ │   Grok   │ │    AI    │ │  Gemini  │ │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘ │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Request Flow

### User Authentication Flow
```
1. User → maula.ai
2. Cloudflare → AWS EC2
3. Nginx → Frontend Container
4. Login → Auth Service (Node.js)
5. Auth Service → MongoDB (Auth DB)
6. JWT Token → User
```

### Tool Usage Flow (Example: IPIntel)
```
1. User → ipintel.maula.ai
2. Cloudflare → AWS EC2
3. Nginx → IPIntel Frontend Container
4. User inputs IP address
5. Frontend → IPIntel Backend API
6. Backend → Python ML Engine (IP Analysis)
7. Backend → MongoDB (IPIntel DB - save results)
8. Results → Frontend
9. User clicks "Ask AI"
10. Frontend → AI Assistant Container
11. AI Container → LLM Router → (Claude/GPT/Grok)
12. LLM Response → AI Container
13. AI Container → Frontend (chat interface)
14. Chat history → MongoDB (IPIntel DB)
```

### API Integration Flow (External Developer)
```
1. Developer → api.maula.ai/ipintel/check
2. Cloudflare → AWS EC2
3. Nginx → API Gateway
4. API Gateway → IPIntel Backend
5. Backend → ML Engine
6. Results → API Gateway → Developer
```

---

## 🗂️ Deployment Strategy

### Single Tool Deployment Unit
Each tool runs as an isolated Docker container set:
```
┌────────────────────────────────┐
│   Tool Container Group         │
├────────────────────────────────┤
│ 1. Frontend (Next.js)          │
│ 2. Backend (Node.js)           │
│ 3. ML Engine (Python)          │
│ 4. AI Assistant (Node.js)      │
│ 5. Redis Cache                 │
└────────────────────────────────┘
       │
       ▼
   MongoDB Atlas
   (Tool-specific DB)
```

### Scaling Strategy
- **Horizontal Scaling**: Add more EC2 instances
- **Load Balancing**: Nginx distributes traffic
- **Database**: MongoDB Atlas auto-scales
- **Caching**: Redis for frequently accessed data
- **CDN**: Cloudflare caches static assets

---

## 🔐 Security Layers

```
Layer 1: Cloudflare (DDoS, WAF)
Layer 2: Nginx (Rate limiting, IP filtering)
Layer 3: Auth Service (JWT, OAuth2)
Layer 4: OpenVPN (Admin access only)
Layer 5: Container Isolation (Docker)
Layer 6: Database (Encrypted at rest, SSL/TLS)
Layer 7: API Keys (Per-tool authentication)
```

---

## 🌍 Domain Structure

```
maula.ai               → Main Dashboard
api.maula.ai           → Unified API Gateway
docs.maula.ai          → Documentation
admin.maula.ai         → Admin Panel

# Individual Tool Subdomains
fraudguard.maula.ai    → Tool 1
deviceprint.maula.ai   → Tool 2
ipintel.maula.ai       → Tool 11
... (50 tools)

# API Endpoints
api.maula.ai/fraudguard/
api.maula.ai/deviceprint/
api.maula.ai/ipintel/
... (50 APIs)
```

---

## 📦 Container Orchestration

### Docker Compose (Development)
```yaml
services:
  - nginx
  - frontend-dashboard
  - auth-service
  - api-gateway
  - tool-1-frontend
  - tool-1-backend
  - tool-1-ml
  - tool-1-ai
  - ... (50 tools × 4 containers)
  - redis
  - mongodb (local dev)
```

### Production (Kubernetes or Docker Swarm)
- Auto-scaling based on load
- Health checks & auto-restart
- Rolling updates (zero downtime)
- Service discovery

---

## 📊 Monitoring & Analytics

```
┌─────────────────────────────────┐
│   Monitoring Stack              │
├─────────────────────────────────┤
│ - Prometheus (Metrics)          │
│ - Grafana (Dashboards)          │
│ - ELK Stack (Logs)              │
│ - Sentry (Error tracking)       │
│ - Uptime Robot (Availability)   │
└─────────────────────────────────┘
```

---

## ⚡ Performance Optimization

1. **CDN**: Static assets via Cloudflare
2. **Caching**: Redis for API responses
3. **DB Indexing**: MongoDB optimized queries
4. **Lazy Loading**: Frontend loads on demand
5. **API Rate Limiting**: Prevent abuse
6. **WebSocket**: Real-time AI chat (no polling)
7. **Compression**: Gzip/Brotli enabled

---

**Next:** Frontend Architecture Design
