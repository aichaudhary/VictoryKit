/**
 * Models Index
 * Export all models for easy importing
 */

const NetworkDevice = require("./NetworkDevice");
const NetworkAlert = require("./NetworkAlert");
const TrafficLog = require("./TrafficLog");

module.exports = {
  NetworkDevice,
  NetworkAlert,
  TrafficLog
};
